# 대시보드 개선 사항 리스트

남은 과제만 정리. 완료되어 제외된 항목은 git 히스토리 참조.

## 0. 게임 스타일 입히기 — 다음 차례
밑작업은 끝났다(2026-08-10). 보이는 값은 전부 `dashboard/theme.css` 의 토큰
한 곳에 있고, `dashboard.css`(구조)와 `dashboard.js`(차트)는 그 값을 읽기만
한다 — 색·서체 리터럴이 두 파일에 남아 있지 않다(검증: var() 참조 74개 전부
정의됨, 미사용 토큰 0).

실제로 입힐 때 손볼 자리는 theme.css 머리 주석에 적어 뒀다. 요지만:
- `--font-ui` 를 Dokkaebi DNR Gothic 으로. 이때 `--fs-*` 를 rem 소수에서
  정수 px 로 바꿔야 한다 — 도트 서체는 16의 배수가 아닌 크기에서 획이 뭉갠다.
  그러라고 글자 크기를 8개 토큰으로 모아 뒀다.
- `--img-rendering: pixelated`, `--radius-*: 0`
- 9-슬라이스 프레임은 토큰만으론 안 되고 `.card` 규칙 하나가 더 필요하다
  (world/ui.css 의 `.frame-box` 와 같은 방식, theme.css 주석에 4줄로 적힘).
- 차트 기하(`--bar-radius`, `--line-tension`, `--spark-line-w` 등)도 토큰이라
  픽셀 톤에 맞춰 각지게 바꿀 수 있다.

## 1. GA와의 차별점이 얇다 — 가장 큰 과제
현재 GA4 화면과 실질적으로 다른 건 셋뿐:
호스트 3개(atah.io / pc98 / suiko) 분리, 경로를 코너 이름으로 묶기,
로컬(localhost·사설 IP) 방문 제외. 나머지는 GA가 이미 하는 일의 재탕이다.

갈라지려면 **GA가 구조적으로 알 수 없는 것**을 넣어야 한다. GA는 이 레포를
모른다 — 언제 무엇을 만들었는지, `/sweep`이 미니게임인지 알 방법이 없다.
후보(검토만 하고 보류):
- **커밋 × 방문 겹쳐 보기** — 추세선에 그날의 커밋을 표시해 "뭘 만들었더니
  사람이 어떻게 움직였나"를 보여준다. `git log`만 있으면 되고 구현이 가볍다.
- **평소와 다른 날만 짚기** — 28일 분포에서 벗어난 날·유입원만 문장으로.
  지금의 인사이트 문장은 1위/비중 나열이라 판단이 없다.
- **미니게임 이벤트 계측** — 시작·클리어·이탈을 GA4 커스텀 이벤트로 심고
  게임별 완주율을 본다. 가장 값지지만 각 게임에 코드 삽입이 필요하다.

검토 후 **접은** 후보 둘(2026-08-10):
- **인구통계(연령·성별)** — 못 쓴다. 이 속성에 직접 물어보니 `userAgeBracket`·
  `userGender`·`brandingInterest` 세 차원 모두 rowCount 0, 완전히 빈 응답이다.
  Google 신호 데이터가 꺼져 있거나 트래픽이 임계값 아래라서인데, 어느 쪽이든
  지금 규모(7일 활성 96명)로는 켜도 대부분 가려진다. 도시·지역·언어·브라우저·
  OS 는 값이 잘 나오지만 그건 GA에서 그대로 보면 되는 것들이라 옮길 이유가 없다.
- **페이지별 상세 쪼개기** — 페이지×기기/채널/신규재방문, 랜딩페이지 이탈률
  모두 API로 잘 나오는 걸 확인했다. 다만 GA 화면에서 바로 볼 수 있는 내용이라
  여기 옮겨 담을 값이 없다고 판단했다.

## 2. 레이아웃 겹침 — 원인 미확정
카드 테두리를 걷어내고 여백 구조로 바꾸면서, 표 8곳에 `overflow-x: auto`
래퍼와 그리드 칸 `min-width: 0`, 캔버스·SVG `max-width: 100%`를 넣어 두었다.
다만 겹침의 실제 원인은 확인하지 못했다 — 작업 당시 브라우저로 열어 볼
수단이 없어 "표가 칸을 넘쳐 옆 구획을 덮는다"는 가설로만 막아 둔 상태다.
다시 겹치면 어느 구획인지부터 확인할 것.

## 3. 안 한 채로 남겨 둔 잔손질
스킨 작업과 섞으면 원인을 가리기 쉬워 밑작업에서 일부러 뺐다. 스킨을 얹은
뒤에 판단할 것:
- **데이터가 오래돼도 티가 안 난다** — 워크플로우가 실패하면 옛 숫자가
  그대로 남고 화면은 아무 말도 하지 않는다. `meta.updatedAt` 이 이틀 이상
  지났으면 머리말에 한 줄.
- **Chart.js CDN 실패** — 못 불러오면 차트 자리가 빈 칸으로 남는다. 표는
  이미 옆에 있으니 안내 한 줄이면 된다.
- **탭이 하나뿐일 때** 탭바에 고를 것이 없다(블로그 속성이 붙기 전 상태).
- **순위 표에 비중 막대** — 국가·인기 페이지는 숫자만 있어 크기 차가 늦게
  읽힌다. 칸 뒤에 옅은 막대를 깔면 순위가 먼저 보인다.
- **인사이트 문장의 숫자 강조** — CSS에 `.insights b` 스타일은 있는데 수집
  스크립트가 굵게 표시를 넣지 않아 실제로는 안 쓰인다. 문장을 그대로 이스케이프해
  넣고 있어서, 굵게 하려면 파이썬에서 텍스트와 강조 자리를 나눠 보내야 한다.

## 4. 갱신 주기
매일 07:00 KST 1회. 이 시각엔 당일치가 거의 비어(28일 실측: 00~06시가 전체
세션의 15%) "오늘" 수치를 쓸 수 없어 어제 하루를 기준으로 삼고 있다.
실시간에 가까운 카운터를 원하면 주기를 늘려야 하고, 그만큼 데이터 파일
자동 커밋도 늘어난다(3시간마다 = 하루 8건).
