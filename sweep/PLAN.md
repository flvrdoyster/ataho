# 술창고 청소 (sweep) — 작업 계획

취호전의 술창고 청소 미니게임 재현. 아타호가 술집주인의 지시로 창고 바닥의 먼지를 쓸어내며
외상 술값을 갚는다. (레퍼런스: 취호전 원작 영상)

## 게임 규칙 (레퍼런스 분석)

- 격자형 방바닥에 먼지 낀 타일이 깔려 있고, **밟고 지나간 칸이 깨끗해진다**
- **빗자루질은 한 칸 이동할 때마다 무조건 1회** (먼지 없는 칸을 밟아도 이동해 왔으면 1회) + 효과음 재생
- 술통·상자·선반 등 장애물이 동선을 막는다
- 방의 먼지를 전부 지우면 클리어 → 다음 방으로 (여러 방 연속)
- 방 클리어 시 보수(번 돈) 지급 — **걸음 수가 최단 루트에 가까울수록 보수가 높다**
- 클리어 후 술집주인/아타호의 평가 대사

## 기술 베이스

- 엔진: `world/engine.js` (타일 렌더·카메라·충돌·트리거·말풍선 UI·게임 루프)
- 캐릭터: `sweep/char_sweep.js` — 술창고 전용 컨트롤러 (idle → moving → sweeping 상태 기계)
  - `window.GRID_CELL_TILES = 3` → **한 칸 = 48px (3×3 맵타일 슬래브)**
  - 근거: 새로 찍은 맵의 바닥 판석이 3×3 타일 반복 블록, 캐릭터 스프라이트 폭(48px)과 일치
  - `window.GRID_ORIGIN = {x:2, y:2}` — 플레이필드 좌상단 슬래브. startPos가 격자에서 어긋나면
    컨트롤러가 가장 가까운 빈 칸으로 자동 스냅
- 맵: `world/maps/souko/` — 베이스 맵 1장 (37×21 타일, 플레이필드 **11×6칸**, gx2~34 / gy2~19)
  + 스테이지별 장애물 조합(방해물 스프라이트)만 교체
- 셀 상태 런타임: `sweep/stages.js`(데이터) + `sweep/loader.js`(먼지 + 스테이지 적용)
  - **먼지**: `DUST_TILE_MAP`으로 깨끗한 바닥 → 같은 돌 실루엣의 이끼 낀 변형 타일(+3칸 자리) 매핑
    - `window.onCellSwept(gx,gy)`(char_sweep.js가 호출) → 밟은 칸을 깨끗한 상태로 전환
    - `window.initDust()` / `window.sweepGetDustRemaining()` — 초기화 / 클리어·HUD용 조회
  - **스테이지**: 베이스 맵 1장 + 스테이지별 **장애물 배치·스폰**만 교체. 좌표는 셀(cx 0~10, cy 0~5)
    - 장애물 스탬프 = 3×3 타일 묶음(`SWEEP_OBSTACLES`). 셀에 놓으면 충돌 등록(이동 차단+먼지 제외)
    - `?stage=N`(1-based) URL 파라미터로 특정 방 테스트. 클리어 시 다음 방 자동 로드
  - `window.getFloorTileOverride` 최종 합성: **장애물 → 먼지 → 원본 바닥** (loader.js가 소유)
- 도구: `sweep/stage_editor.html` — 스테이지 저작 에디터, `sweep/calc_minsteps.js` —
  스테이지별 정확한 최단 걸음 수 계산(Node 실행, 방 배치 수정 시 재실행해 `stages.js` 갱신)
  - 베이스 맵 위 11×6 셀 그리드에 장애물 스탬프 클릭 배치 / 지우개 / 스폰 지정
  - "스탬프 정의" 모드: 3×3 슬롯 + 타일셋 팔레트로 새 장애물 스탬프 제작
  - 방 탭(추가/삭제/전환), "이 방 테스트"(index.html?stage=N), "stages.js 복사"(Export)
  - `sweep/reference_rooms.png` — 원작 5개 방 장애물 배치 참조 캡처
- 스프라이트: `world/char/ataho-walk.png`(이동), `world/char/ataho-sweep.png`(빗자루질 48×64 3프레임, 정면)
  - 빗자루질 모션은 **매 이동 후 1회 재생** + 효과음 (`sweep/sweep.mp3`)
  - 스프라이트는 판석 하단보다 한 타일(16px) 위에 그려짐 (`SPRITE_OFFSET_Y: -16`)
- 진입점: `sweep/index.html` (파일 직접 열기로 동작, `MAP_BASE='../world/'`)

### 레퍼런스 실측 타이밍 (2026-07-10, 영상 60fps 프레임 차분 분석)

- 원작 게임 틱: **20Hz** (60fps 영상에서 3프레임마다 화면 변화)
- 한 칸 이동: 24px(원본 해상도)를 8px×3틱 = **약 0.18초**
- 빗자루질: 이동 직후 제자리 4틱 = **약 0.27초** (스윙 3프레임 + 복귀)
- 키 홀드 시 스텝 주기: **한 칸당 약 0.45초** (26~27프레임 간격으로 일정)
- `char_sweep.js`의 `MOVE_DURATION: 0.18` / `SWEEP_DURATION: 0.27`로 반영

## 작업 스텝

- [x] **게임 셸 + 그리드 이동 검증**
  - souko 베이스 맵 초벌 (30×24 타일, 내부 바닥 13×9칸, 벽·문·collision·startPos)
  - 범용 `world/char/char_grid.js`로 그리드 이동 1차 검증 (이후 술창고 전용
    컨트롤러로 교체하며 삭제 — 처음부터 이 게임 전용으로 만든 것이라 범용화할 이유가 없었음)
  - `sweep/index.html` 셸 생성 — 아타호가 한 칸씩 이동, 벽에 막힘
  - 맵 초벌은 에디터에서 다듬기 (담당: 오이스터)
- [x] **캐릭터 움직임 구현**
  - 레퍼런스 영상 실측 타이밍 반영 (이동 0.18s + 빗자루질 0.27s ≈ 0.45s/칸)
  - `sweep/char_sweep.js` 전용 컨트롤러로 교체: 키 홀드 연속 이동, 막히면 방향 전환만(빗자루질·효과음 없음)
  - 매 이동 후 빗자루질 1회 + 효과음. 먼지 시스템 연결 지점 `window.onCellSwept(gx, gy)` 마련
  - 빗자루질 효과음 `sweep/sweep.mp3` 전용 효과음으로 교체 완료
  - 터치(모바일) 입력 동일 동작 (검증 필요)
  - 데스크톱 이동·빗자루질 동작 확인 완료 (2026-07-10)
- [x] **먼지 시스템**
  - 타일 자체를 교체하는 방식 채택 (오버레이 아님) — souko_tile.png에서 깨끗한 바닥과
    동일 실루엣의 이끼 낀 변형 타일 짝을 찾아 매핑
  - engine.js에 `getFloorTileOverride` 훅 추가 (미정의 시 기존 동작과 완전히 동일, 하위 호환)
  - 먼지 로직(걸을 수 있는 칸 전체를 초기 먼지로 채움 + `onCellSwept`로 밟은 칸 즉시 청소).
    처음엔 `dust.js`로 분리했다가 나중에 `loader.js`에 통합 (아래 결정 로그 참고)
  - 시작 칸은 처음부터 깨끗한 상태로 시작
  - 시뮬레이션 렌더로 검증: 66개 바닥칸 전체 이끼 표시 확인, 지그재그 경로 청소 시
    밟은 칸만 정확히 깨끗해짐 확인 (2026-07-10)
- [x] **방 클리어 & 스테이지 진행**
  - 스테이지 데이터 구조 확립 (`stages.js`) — 베이스 맵 재사용, 장애물 조합·스폰만 교체 ✔
  - 런타임 적용 (`loader.js`) — 장애물 충돌/렌더, 먼지 자동 제외, 스폰 배치 ✔
  - "먼지 0 = 클리어" 판정 → 다음 방 자동 로드 (연출·보수는 이후 단계에서) ✔
  - 스테이지 저작 에디터(`stage_editor.html`) 제작 + 브라우저 검증 ✔ (2026-07-11)
  - 원작 5개 방 장애물 배치 참조 캡처 제공 (`reference_rooms.png`) ✔
  - 에디터로 실제 5개 방 배치 완료 (오이스터, 스탬프 6종: 포대1/2·술박스·작은박스더미·
    세운술통·누운술통) — 장애물 수 15/11/16/18/28개, 전부 스폰에서 전 칸 도달 가능 검증 완료 ✔
  - 방 관리 확장 (2026-07-16): 각 스테이지에 `id` 부여 — 원작 재현은 `o_1`~`o_5`,
    AI 생성은 `a_#`. 게임 로직상 구분 없이 전부 무작위 후보(관리용 태그일 뿐).
    - ~~a_1~a_5는 Claude가 설계 + 연결성 검증 후 stages.js에 추가~~ → 오이스터가 에디터로
      확인 후 품질 미달 판단, 전부 삭제 (2026-07-16). 현재 `o_1`~`o_5`만 존재
    - ~~**TODO**: `stage_editor.html`의 `serialize()`가 `id`/`minSteps`를 안 챙겨서
      저장/복사하면 날아감~~ → `serialize()`가 두 필드 모두(있으면) 다시 써넣도록 수정
      완료 (2026-07-16), 실제 `stages.js` 데이터로 라운드트립 검증 ✔
    - "+ 방"으로 새로 추가하는 방은 `id`가 아예 없었던 것도 수정 — "+ 방"은 레퍼런스
      영상을 보고 원작 방을 추가 재현하는 용도라 `o_N`을 이어서 자동 부여하도록 함
      (2026-07-16) ✔
    - 새 레퍼런스 영상(`sweep/ref2.mov`, 원작 실플레이 9개 방)에서 발견한 신규 배치 7개를
      오이스터가 에디터로 직접 재현해 `o_6`~`o_12`로 추가 (2026-07-16). `calc_minsteps.js`로
      전부 minSteps 계산 완료(55/49/45/47/51/47/41) — 원작 재현 방 총 12개 ✔
    - `calc_minsteps.js`/`stages.js`에 `hamiltonian`(해밀턴 경로 여부) 필드 추가 —
      되밟기 α=0이면 true. `o_1`/`o_3`만 false(각 1칸 되밟기 필요), 나머지 10개는 true.
      `stage_editor.html`의 `serialize()`도 이 필드를 보존하도록 함께 수정 (2026-07-16) ✔
    - 오이스터가 에디터로 `o_13`~`o_16` 4개 방 추가 (2026-07-16). `calc_minsteps.js`로
      minSteps/hamiltonian 계산 완료(56/true, 46/false, 26/true, 47/true) ✔
    - `o_17`~`o_19` 3개 방 추가 (2026-07-17), minSteps/hamiltonian 계산 완료
      (46/false, 55/true, 50/true) — 원작 재현 방 총 19개 ✔
- [x] **걸음 수 채점 & 보수**
  - 걸음 수 카운트 (`char_sweep.js` — `player.stepCount`, 성공 이동마다 +1, 스테이지 로드 시 리셋,
    `window.sweepGetStepCount()`로 조회) ✔
  - 스테이지별 최단 걸음 수 — 처음엔 트리 공식(BFS)으로 자동 계산했으나 실제 최적의
    1.6~1.8배로 부풀려져 절반 성공이 안 나오는 문제 발견(2026-07-16). 저작 도구
    `calc_minsteps.js`(완전 탐색, 정확값)로 계산해 `stages.js`의 `minSteps`에 저장하는
    방식으로 교체. `window.sweepGetMinSteps()`는 저장값 조회 ✔
  - 등급 → 보수 차등 지급 (`loader.js` — 최단 걸음 수 대비 **절대 걸음 수 차이**로 판정:
    완벽=최단 그대로 / 성공=최단+3 이내 / 절반 성공=그 초과. 완벽·성공 100G, 절반 성공 75G.
    스테이지 클리어 시 `char_sweep.js`가 `window.sweepGetStageResult(stepCount)` 호출 →
    `window.sweepAddMoney()`로 누적, `window.sweepGetTotalMoney()`로 조회) ✔
  - HUD (번 돈 / 걸음 수) — `world/ui.css`+`ui.js`(`UIStat`) 재사용해 balance와 동일 패턴으로
    구성 (`index.html`에 `#hud`/`#hud-stats` 마크업 + `../world/ui.css`/`ui.js` 로드,
    `char_sweep.js`의 `setupHUD()`/`updateHUD()`) ✔
- [x] **연출 마무리**
  - 술집주인 등장 타이밍 구현 완료 (2026-07-16) — `char_sweep.js`:
    - 최단 걸음 수 이내(또는 딱 넘겨서 클리어)로 클리어하면 **클리어 시점**에 문 앞 등장
    - 최단 걸음 수를 넘기면 그로부터 `MASTER_OVERSHOOT_STEPS`(2)걸음 더 걸었을 때 등장,
      이후 방을 마칠 때까지 그 자리 고정 — `ref2.mov` 재검증(방2 t≈76~82s, 아타호 반응
      대사 "알았어, 알" + 문 앞 등장 프레임)으로 확인한 규칙
    - 등장 위치: 스폰 칸(`{cx:5,cy:5}`) 바로 아래 문 칸, `doorWorldPos()`
    - 대사 큐(`queueSay([{text, speaker}, ...])`) 추가 — 말풍선 하나가 닫혀야(자동
      2초 또는 수동 스킵) 다음 줄이 뜨는 순차 재생. `char_sweep.js`의
      `advanceDialogueQueue()`를 매 프레임 호출
    - 대사 5곳에 배치 완료(실제 대사로 교체됨 — 원작 번역 데이터 기준):
      1) 첫 등장(세션당 1회, 첫 방 진입 시 이미 문 앞에 서 있는 인트로)
      2) 최단 걸음 수 초과 시(문 앞 등장 + 아타호 반응)
      3) 절반 성공 구간 진입 시(등급이 실시간으로 'half'가 되는 첫 순간, 한 번 더 재촉)
      4) 클리어 시 — 완벽/성공/절반 성공 각각 술집주인+아타호 주고받기
    - 첫 등장(인트로) 대사가 끝나면 퇴장 연출 — `master_all.png`(32×64 2프레임: 정면/
      뒷모습 가로 배치)로 교체, `queueSay`에 `onComplete` 콜백을 추가해 마지막 대사가
      닫히면 `startMasterLeave()` 호출 → 뒷모습으로 전환 → `MASTER_LEAVE_DELAY`(0.6s)
      후 사라짐. 다른 등장(초과/클리어)은 그대로 방이 끝날 때까지 정면으로 고정
    - 말풍선 버그 수정: 걸음마다 깜빡이던 것(빗자루질 분기가 `return`으로 `drawMaster`
      호출을 건너뜀 → 순서 재배치), 방향키 홀드 시 대사가 뜨자마자 스킵되던 것(엔진의
      키 리핏이 대사 넘기기로 오인됨 → `e.repeat` 무시), 모바일에서 화자가 캔버스 밖일 때
      말풍선도 같이 잘리던 것(`world/engine.js`에 `window.getBubbleAnchor` 훅 추가 +
      화면 뷰포트 기준 클램프, 꼬리는 `--bubble-tail-x`로 화자 쪽 추적) — 전부 반영
    - 말풍선 최대 폭을 임의값(250px)에서 한글 20자 기준(`min(20em, 85vw)`)으로 변경
    - 다음 스테이지 전환 대사 추가 — 평가 대사가 끝나면 누적 번 돈 기준으로 분기
      (`CHAR_CONFIG.MONEY_GOAL`(1000G) 미달: "다른 방으로 가세!" / 달성: "외상 술값만큼의
      일은 끝났네!!"), 전환 대사까지 닫힌 뒤에 다음 방으로. 현재는 두 분기 모두 무한 계속
    - 스테이지 전환 페이드 인/아웃 추가 — 전환 대사가 끝나고 `STAGE_CLEAR_DELAY` 지나면
      화면이 검게 페이드아웃(`STAGE_FADE_DURATION`, 기본 0.3s)된 뒤 그 타이밍에 방을
      교체하고, 다시 페이드인. `#stage-fade`(fixed 오버레이) opacity를 매 프레임 dt로 직접
      계산 — CSS transition을 안 쓴 이유는 게임 dt와 타이밍이 어긋나지 않게 하기 위함
  - 술집주인 스프라이트 `sweep/master_all.png`(32×64 2프레임: 정면/뒷모습) 사용 중
  - 사이트 연결 완료 ✔
- [ ] **전체 클리어 목표**
  - 1000G 달성 시 "계속할텐가?" 선택지 → 그만두면 엔딩 연출 (전환 대사 분기는 이미
    구현돼 있음, 선택지·엔딩만 남음)
- [x] **배경음악 & 뮤트**
  - BGM(`bgm.mp3`) 루프 재생 + 클리어 등급별 효과음(완벽·성공 `success.mp3` /
    절반 성공 `failure.mp3`, `CHAR_CONFIG.CLEAR_SFX` 매핑) 추가 ✔
  - 술집주인 등장/퇴장 공용 효과음(`door.mp3`) 추가 — 실제로 화면에 나타나거나
    사라지는 순간에만 재생(이미 등장해 있는데 클리어된 경우는 재생 안 함) ✔
  - 오디오 끊김 방어는 haiyuki(assets.js) 전략 이식: 자동재생 거부 시 사용자 제스처마다
    재시도, iOS 복귀 신호(visibilitychange/focus/pageshow)에서 재개, 핸들러 once 없이 유지 ✔
  - 효과음(빗자루질/클리어/문)이 자주 씹히는 문제 발견 → HTMLAudioElement 방식이 원인
    (iOS 요소별 제스처 언락 부재, 겹침 재생용 풀 관리, currentTime=0 재사용 경합).
    haiyuki(assets.js)처럼 **WebAudio**(AudioContext + AudioBuffer + BufferSource)로
    전면 교체 — BGM만 HTMLAudio 유지, SFX는 `loadSfxBuffer`/`playSfxBuffer`로 통일.
    AudioContext.resume()은 기존 BGM 제스처 핸들러에 얹어서 같이 처리 ✔
    (재발 방지용 메모리 기록: feedback_minigame_sfx_webaudio)
  - 뮤트 버튼 — gensei-pc98/suiko-web 에뮬레이터의 `#btn-mute`(음표 SVG 켜짐/꺼짐 아이콘)를
    그대로 가져와 **공용 `UIMuteButton`**(`world/ui.js`)으로 일반화. 실제 음소거 동작은
    게임마다 오디오 구조가 달라 `onToggle(muted)` 콜백에 위임 — sweep은 BGM pause/play +
    AudioContext suspend/resume(`setAudioMuted`), balance는 기존 게인 0 처리 그대로 연결.
    balance의 PNG 아이콘(`sound.png`/`sound_mute.png`) + 개별 toggleSound 구현은 제거하고
    이 컴포넌트로 교체 ✔
  - 뮤트 버튼 (balance의 sound-btn 패턴 재사용) — 남은 작업
- [x] **모바일 추가 대응**
  - 터치 이동 확인 완료(기존 동작 양호) ✔
  - 미니맵 기능 — 우측 상단에 11×6 셀 격자 미니맵 추가. `loader.js`의
    `window.sweepGetCellGrid()`(obstacleTiles/dustyCells를 매 프레임 그대로 읽어 계산,
    별도 상태 없음)를 `char_sweep.js`의 `setupMinimap()`/`updateMinimap()`이 캔버스에 그림
    (장애물/먼지/청소됨/플레이어 위치 4색). 카메라가 플레이어를 따라다녀 방 전체가 화면
    밖으로 나가는 모바일 세로 화면에서 특히 유용 ✔

## 결정 사항 로그

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-07-10 | ~~한 칸 = 32px (2×2 타일)~~ → **48px (3×3 타일)로 변경** | 새로 찍은 맵의 판석이 3×3 타일 블록. 캐릭터 폭 48px와 1:1 |
| 2026-07-10 | 베이스 맵 1장 + 장애물 조합 교체 구조 | 방마다 맵을 새로 그리지 않음 |
| 2026-07-10 | `souko_tile_top`(천장) / `souko_tile_ana`(큰 문·구멍벽) 이 게임에서 미사용 | 현재 쓰는 바닥·벽 타일로 충분 |
| 2026-07-10 | 이동/빗자루질 타이밍 실측값 고정 | 레퍼런스 60fps 프레임 차분 분석 (위 실측 타이밍 절 참고) |
| 2026-07-10 | 빗자루질은 매 이동 후 무조건 1회 + 효과음 | 원작 동작 확인 (먼지 유무 무관) |
| 2026-07-10 | souko 타일 카테고리를 외벽/바닥/벽/장애물로 재분류 + 에디터에 우클릭 카테고리 지정 기능 추가 | 픽셀 자동분류(rock_wall/water 등)로는 장애물 배치(방 클리어 & 스테이지 진행)에 못 씀 |
| 2026-07-10 | 먼지는 오버레이가 아니라 **타일 자체 교체** 방식 | 같은 돌 실루엣의 이끼 낀 변형 타일이 타일셋에 이미 존재(+3칸 자리) — 별도 에셋 불필요 |
| 2026-07-11 | 장애물/스테이지 저작은 **전용 에디터(`stage_editor.html`)** — 기존 맵 에디터 확장 아님 | 셀(3×3) 단위 오버레이 모델이 타일 단위 단일맵 모델의 맵 에디터와 안 맞음. 범용 도구를 sweep 전용 복잡도로 오염시키지 않음 |
| 2026-07-11 | 장애물 = 셀에 놓는 3×3 타일 스탬프, 충돌은 런타임에 등록 | 베이스 맵 collision을 건드리지 않고 스테이지마다 교체 가능. 먼지도 walkable 기준이라 자동 제외됨 |
| 2026-07-11 | `dust.js`를 `loader.js`에 통합 (파일 하나로) | 둘이 바닥 override 훅을 통해 강하게 결합돼 있었고, 분리 탓에 생긴 speculative 폴백(`loader.js` 미로드 대비)이 사족이었음. 합쳐서 훅 핸드오프·로드순서 의존 제거 |
| 2026-07-16 | ~~최단 걸음 수는 저장하지 않고 매 계산(BFS 트리 공식)~~ → **`calc_minsteps.js`(완전 탐색)로 정확값을 계산해 `stages.js`에 저장** | 트리 공식은 순환 경로 많은 격자에서 실제 최적의 1.6~1.8배로 부풀려져(방1: 공식 88 vs 실제 51) 절반 성공이 사실상 안 나왔음. 정확값은 공식이 없어(커버링 워크 — 해밀턴 경로 존재부터 NP-완전) 완전 탐색이 필요한데, 이건 런타임에 돌릴 게 아니라 저작 시점에 돌릴 물건. stale 우려는 도구가 stages.js 값과 대조해 갱신 필요를 알려주는 것으로 보완 |
| 2026-07-16 | 등급(완벽/성공/절반 성공) 판정은 **비율이 아니라 최단 걸음 수 대비 절대 걸음 수 차이** | 완벽=최단 그대로 / 성공=최단+3 이내 / 절반 성공=그 초과. 완벽·성공 100G, 절반 성공 75G (레퍼런스 기준). 한때 +2로 조였으나 그건 minSteps가 부풀려져 있던 시절 보정 — 정확값 도입과 함께 +3으로 복원 |
| 2026-07-16 | 방 클리어 후 다음 방은 **순차가 아니라 무작위 선택** (직전 방은 후보에서 제외) | 방 사이에 난이도·순서 설계가 없음 |
| 2026-07-16 | 시작 방도 `?stage=N` 없으면 무작위 선택 (파라미터 있으면 그대로 존중) | 클리어 후 다음 방 선택과 같은 이유로 방 순서 설계가 없음. `?stage=N`은 `stage_editor.html`의 "이 방 테스트"가 특정 방을 지정할 때 쓰므로 유지 |
| 2026-07-16 | 술집주인 등장은 **성적 기반**: 최단 이내 클리어=클리어 시점 등장, 초과=초과 후 2걸음 뒤 등장(이후 고정) | `ref2.mov` 프레임 단위 재검증(방2: t≈76s까지 없음 → t≈80s 아타호 반응 대사 → t≈82s 문 앞 등장, 이 방은 최단 초과·성공 등급). 첫 방 도입부에 이미 서 있던 건 별개(인트로용, 성적과 무관) — 이번 구현은 성적 기반 규칙만 반영 |

## 참고

- `souko_tile_top.png`(천장), `souko_tile_ana.png`(큰 문·구멍 난 벽) — **이 게임에서 미사용**
